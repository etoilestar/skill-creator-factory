"""Placeholder for future Skills-level chat endpoints.

当前模块仅占位，后续可在此添加 /api/chat/skills/{name} 等路由。
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/chat/skills", tags=["chat-skills"])

# TODO: Add skills-scoped chat endpoints when model-driven skill chat is needed.
